DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/bjj_checkin"
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Custom domain for the public gym site (no protocol, no trailing slash)
# e.g. mygym.com — leave empty to serve site at /site
NEXT_PUBLIC_SITE_DOMAIN=

# Email and SMS are configured per-gym in Settings → Email & SMS (Brevo).
# Gym owners sign up at https://brevo.com and enter their API key there.
